params {
    input_csv = "${launchDir}/test/test-bam2fastq/input.csv"
    pipeline_work_dir = '/scratch/\$SLURM_JOB_ID'
    save_intermediate_files = true
    patient = 'GZLUCPTA000001'
    project_id = 'LUAD0000034'
    sample_mode = 'paired'
    
    convert_BAM2FASTQ {
        subworkflow_cpus = 2
        get_bam_stats_SAMtools_cpus = 2
        collate_bam_SAMtools_cpus = 2
    }

    arg_map = [:]
    params.convert_BAM2FASTQ.each{ param_key, param_value -> {
            arg_map[param_key] = param_value
        }
    }
    params.convert_BAM2FASTQ.metapipeline_arg_map = arg_map
}
