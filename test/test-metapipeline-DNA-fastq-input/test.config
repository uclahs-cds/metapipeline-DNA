includeConfig "${projectDir}/config/default.config"
includeConfig "${projectDir}/config/methods.config"
includeConfig "${projectDir}/nextflow.config"

params {
    input_csv = "${projectDir}/test/test-metapipeline-DNA-fastq-input/input.csv"
    output_dir = "${projectDir}/test/output"
    leading_work_dir = "${projectDir}/test/output/leading_tmp_dir" // Should be a /hot/path | Can't use /scratch
    pipeline_work_dir = "/scratch" // For individual pipeline outputs before copying to output_dir
    project_id = 'GZLUCPTA000001'
    save_intermediate_files = false
    metapipeline_DNA_config = "${projectDir}/test/test-metapipeline-DNA-fastq-input/meta-pipeline.config"

    partition = 'F72'
    clusterOptions = '--exclusive'
    per_job_cpus = 72
    per_job_memory_GB = 136.8
    max_parallel_jobs = 5
}

methods.set_up()
